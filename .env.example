# Shared environment variables example
# Copy to .env and fill in actual values

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# API Configuration
API_URL=http://localhost:8000

# Optional: Custom Hugging Face model
HF_MODEL_ID=nateraw/food

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# A/B Testing Configuration
# Set to "true" to enable random preprocessing strategy selection for A/B testing
# Default: "false" (uses quality-based selection)
ENABLE_AB_TESTING=false

# AI-Powered Health Tips Configuration
# Set to "true" to enable AI-powered health tips (default: true)
# Falls back to rule-based tips if disabled or API fails
ENABLE_AI_TIPS=true

# AI Provider Selection (default: openai recommended for best experience)
# Options: huggingface (requires FREE token), openai (paid, best), anthropic (paid)
AI_TIP_PROVIDER=openai

# HuggingFace API Key (REQUIRED if AI_TIP_PROVIDER=huggingface)
# Get FREE key at: https://huggingface.co/settings/tokens (no credit card needed)
# Create a "Read" token - completely free with rate limits (10K/hour)
HF_API_KEY=hf_xxxxx

# OpenAI API Key (RECOMMENDED - best quality, ~$0.00015 per request)
# Get key at: https://platform.openai.com/api-keys
# Uses gpt-4o-mini model
OPENAI_API_KEY=your-openai-key-here

# Anthropic API Key (Only if AI_TIP_PROVIDER=anthropic)
# Get key at: https://console.anthropic.com/
# Uses Claude 3.5 Haiku (~$0.0001 per request)
ANTHROPIC_API_KEY=